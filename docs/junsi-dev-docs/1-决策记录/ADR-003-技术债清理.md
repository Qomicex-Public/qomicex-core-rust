# ADR-003：技术债清理（TD-1 ~ TD-8）

- 日期：2026-08-06
- 状态：已确认（8 项全部处理，35 测试通过，零警告）

## 处理结果

| # | 技术债 | 处理 | 验证 |
|---|--------|------|------|
| TD-1 | Error::Http 无 status 字段 | ✅ Http 变体加 `status: Option<u16>`（157 处构造点适配）；CurseForge 非 2xx 填充真实状态码；is_bad_request 改结构化 `matches!(status: Some(400))` | 编译 + 测试 |
| TD-2 | Windows DNS ipconfig 近似 | ✅ 注册表优先（HKLM\...\Tcpip\Parameters\Interfaces 的 NameServer/DhcpNameServer，winreg crate，与语言无关）+ ipconfig 兜底 | 编译 + 测试 |
| TD-3 | SRV 端口丢失 | ✅ resolve_status_endpoint 直接走 resolve_srv_internal（保留 (target, port)），connect_port 用 SRV 端口替代 25565 | 编译 + 测试 |
| TD-4 | Directory 创建失败静默 | ✅ locator 的 create_dir_all 失败改 eprintln 日志 | 编译 |
| TD-5 | datetime ISO 子集 | ✅ 定案：保持子集（真实数据不触发），文档化 | 文档 |
| TD-6 | NBT HashMap 键序随机 | ✅ util/nbt.rs 换 IndexMap（保插入序，写回顺序稳定） | 编译 + 测试 |
| TD-7 | QOMICEX_HOME 宿主约定 | ✅ README Android 段文档化 | 文档 |
| TD-8 | CacheExpiry/MaxConcurrentDownloads 未透传 | ✅ VersionManagementService::new 加 2 参数 → VersionManifestCache 有效期参数化 + completer 并发数参数化；builder 透传（默认 5 分钟/8 保留） | 编译 + 测试 |

## 附带修复

- README 的 rebase 冲突标记残留（`>>>>>>> 51bfbe0`）清理
- README 更新为移植完成状态 + Android 说明段

## 验证

- `cargo check`：0 error 0 warning
- `cargo test`：35/35
